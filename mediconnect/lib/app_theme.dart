import 'package:flutter/material.dart';

// Shared colours keep the app consistent and easy to restyle.
const Color kPrimary = Color(0xFF0F766E);
const Color kPrimaryDark = Color(0xFF115E59);
const Color kAccent = Color(0xFF38BDF8);
const Color kBackground = Color(0xFFF4F8FA);
const Color kTextDark = Color(0xFF102A33);
const Color kMuted = Color(0xFF64748B);

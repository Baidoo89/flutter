import 'package:flutter/material.dart';

// GCTU-inspired brand colours used across every screen.
// Keeping them here means one edit updates the whole app.
const Color kNavy = Color(0xFF002060);
const Color kAmber = Color(0xFFD4A017);
const Color kSoftNavy = Color(0xFF163B73);
const Color kPageBackground = Color(0xFFF6F2E8);
